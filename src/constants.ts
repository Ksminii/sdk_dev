/** Default flush interval: 3 seconds */
export const DEFAULT_FLUSH_INTERVAL = 3000;

/** Default max queue size before auto-flush */
export const DEFAULT_FLUSH_QUEUE_SIZE = 20;

/** Session idle timeout: 30 minutes */
export const DEFAULT_SESSION_IDLE_TIMEOUT = 30 * 60 * 1000;

/** Session max duration: 24 hours */
export const DEFAULT_SESSION_MAX_DURATION = 24 * 60 * 60 * 1000;

/** sessionStorage key for session data */
export const SESSION_STORAGE_KEY = 'la_session';

/** SDK version */
export const SDK_VERSION = '0.1.0';

/** Max text length to capture from elements */
export const MAX_TEXT_LENGTH = 100;

/** Recording flush interval: 5 seconds */
export const DEFAULT_RECORDING_FLUSH_INTERVAL = 5000;

/** Recording max batch size */
export const DEFAULT_RECORDING_FLUSH_SIZE = 50;
